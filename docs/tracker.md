# Elsewhere — work tracker

The working board until we move to a real ticketing system. Updated as things move. Four workstreams. Newest status at the top of each.

**Legend:** 🔴 blocked-on-Sven · 🟡 in progress · 🟢 done (recent) · ⚪ queued

---

## A. Infra & ops
Numbers, hosting, DB, domains, the teaser plumbing, landing page.

- 🟢 **Recording-ON-for-seed RATIFIED** (2026-07-24, supersedes `concept.md:345` "default off" for the seed phase; folds into `env.ts` recording comment). Dual-channel recording + transcription stays ON. **What's accepted:** callers are all personally-invited seed testers, recordings are used *only* by Sven internally to verify the system works (no training beyond prompt iteration, no marketing, no sharing). Legal read (2026-07-24): at this caller base the two-party-consent exposure is low-probability implied-consent territory; the residual is one organic all-party-state caller (CA §632.7 = a complete \$5k claim), which is *structurally the success metric*. **Expiry triggers — this ratification ends at the FIRST of:** first caller not in the seed list · the Artizen/community post · any press or social mention Sven didn't initiate · payment · hardware in a venue. The community post is the real event horizon (that's when untraceable strangers enter the corpus and consent stops being retrofittable — `concept.md:241`)
- 🟢 **`/legal` = `/privacy` = `/terms` page LIVE-IN-REPO** (2026-07-24, `apps/web/legal.html`, served by the bridge at all three routes; linked from both footers as "The Fine Print"). Two-voice like `/directory`: deadpan fiction frame + a genuinely plain honest section — AI disclosure, what's collected (number/recording/transcript), internal-QA-only purpose, no publish without opt-in, 18+, deletion-on-request, contact. ⚠️ **NOT truly live until (a) it's deployed (`railway up`) and (b) `hello@elsewheretel.co` actually routes** — the deletion channel is legally hollow if that mailbox is dead. Confirm the address (or swap it) before deploy
- 🔴 **BEFORE the community post — remaining mitigation bundle** (2026-07-24). Pre-roll `<Say>` **REJECTED** (Sven: stay in fiction always) → the recording disclosure becomes part of the **Operator's in-character cold-open** when the switchboard ships ("management records all of this — not that anyone expected otherwise… quality assurance… yeah right"), so it lands before the community post by construction. Remaining: (1) **text the seed friends one retro-consent line** while they're still reachable — the only window retrofitting works. F7 log masking: ✅ done (`redact.ts` masks numbers to last-2 everywhere; verbatim utterances gated behind `LOG_TRANSCRIPTS`, off by default — Supabase stays the only full-transcript store). Security hardening (F5/F33): ✅ done (above). Footer word-swap "monitored"→"recorded" NOT done — keeps the canon "never says so" joke (`canon.md:38`); the accurate word "recorded" now lives on `/legal` instead + will be audible in the Operator open (Sven to confirm this is enough, or force the swap). ⚠️ open gap: the standalone **(505) Devil line** doesn't pass through the Operator, so it carries no disclosure — decide whether it stays published on `/directory` or gets its own in-fiction disclosure beat
- 🟢 **Security hardening SHIPPED-IN-REPO** (2026-07-24, F5 + F33; verified w/ unit tests). `X-Twilio-Signature` now validated on all 4 Twilio webhooks (`twilio-auth.ts`, matches Twilio's published vector; multi-host candidate match for the proxy; `TWILIO_VALIDATE=false` escape hatch + mismatch diagnostics). `/media-stream` now requires an HMAC token minted only by our TwiML (`streamToken`), checked on the WS "start" — blocks free Realtime sessions on our key. F33: `guards.ts` adds concurrency cap (`MAX_CONCURRENT_CALLS=8`), rolling daily cap (`MAX_CALLS_PER_DAY=100`), and per-caller rate limit (`MAX_CALLS_PER_CALLER=4`/15min) → in-fiction `<Reject reason="busy">` (busy signal = the rate limit, vision §8). All keyed on the existing `TWILIO_AUTH_TOKEN`, so live on next deploy with no new secrets. ⚠️ **after deploy, make one test call to each line** — if it 403s, a real Twilio request's host didn't match; check `PUBLIC_HOST` or set `TWILIO_VALIDATE=false` while debugging (the mismatch logs the hosts it tried)
- 🟢 **Call-quality fixes SHIPPED-IN-REPO** (2026-07-24, F29 + F30). F29: barge-in now uses a Twilio `mark` queue so an interruption in the *tail* of a reply (after `response.done`, while Twilio is still playing buffered audio) correctly truncates + clears — previously the persona talked over the caller. F30: a fast hang-up now still finalizes its `calls` row and links the `recording_sid` (close() awaits the insert + recording promises instead of dropping the row when the DB round-trip hadn't landed)
- 🟢 Signup capture window FIXED + DEPLOYED (2026-07-24) — teaser is 56s; old single `<Gather>` window (56+4s) missed any "press 1" during the ~7s goodbye. This silently ate EVERY real friend press (Erick +505-204 and ≥1 other both pressed; `signups` was empty). Goodbye now has its own `<Gather>`, main timeout 6s. ⚠️ conversion numbers to date are undercounts
- 🟢 Signup funnel VERIFIED live — press-1 records correctly (`signups` + `teaser_calls.outcome=signup` both landing; confirmed via Sven's own test call). Hardened `setTeaserOutcome` to upsert (was update) so outcome records even if the pickup insert races/fails
- 🟢 **World Graph = the backbone — phased path ADOPTED** (2026-07-24). Source of truth = a git-versioned, `zod`-validated structured graph (worlds/nodes/edges + `node_config` overlay) the bridge walks. **Phase 0** = build the switchboard schema-first + linter in CI (≈0 extra). **Phase 1** = interactive **read-only** React Flow map + basic red/yellow/green status overlay (~3–5d; static Mermaid rejected). **Phase 2** = full production pipeline `production-pipeline.md` (gated content CI/CD: design/linter→assets→produce→sign-off, rollup to staging→production, agent-operable) — SPEC'd now, built later behind triggers. Docs: `engineering/production-tools/`
- 🔴 **v0 Switchboard — designed (arch + experience)**. Build spec `engineering/switchboard-v0.md`; experience/world design `world/switchboard/switchboard.md` (**Switchboard = World Zero**, Operator as a Resident). SETTLED: Devil option hits a **receptionist wall** (current teaser = the Devil's receptionist; no live Devil / no cost yet); SMS deferred (recite URL now); record whole journey (event log + user-journey view, two POVs); Operator pre-produced-now/live-later. Carry infra fix: one ring + confirm connection before VO (fixes cut-off intro). OPEN (brainstorm, `world/switchboard/switchboard.md` §Decisions): menu set/order · Devil-wall copy · fall-through · easter egg · Operator name. Then build phase 1
- 🟢 Teaser analytics — logs EVERY call (teaser_calls: outcome, duration, status, repeat callers via caller_number+created_at). Twilio status callback → duration for dropoff analysis. `/teaser-stats` = calls · unique · repeat · signups · conversion% · avg duration
- 🟢 Teaser timing tuned to Sven's ear — one ring, minimal gap (dropped `<Pause>`, 0.35s lead silence), barge-in "press one at any time"
- 🟢 Teaser line **(806) 666-1212** live — routes, signups table, deployed
- 🟢 Devil line **(505) 551-3551** live (bridge, guardrails, logging, recording)
- 🟢 Railway bridge, Supabase (new keys), Twilio upgraded off trial
- 🟢 Domain **elsewheretel.co** purchased
- 🟢 ElevenLabs Starter active — "Elsewhere Operator" voice saved to library; key uncapped
- 🟢 **Landing page LIVE** — served by the bridge from apps/web (not Vercel; one-system decision). Seal logo (vectorized rotary phone + Special Elite curved text), favicons, click-to-call number, 5 easter eggs. brand-voice tariff card
- 🟢 **`/directory` lore page LIVE** (deployed 2026-07-24) — discoverable via landing footer; `noindex` (flippable to search-public). In-world Directory front-matter + two-voice split (fiction ↔ honest memo). Modern type-scale redesign (70rem canvas, 20px base, fluid scale, multi-column), "old phones" copy removed, em-dash/AIism sweep
- 🟢 **Landing page widened + rescaled** (deployed) — bigger hero tagline, larger number, 50rem frame, legibility bumps. Per type-scale research
- 🟢 **elsewheretel.co LIVE over HTTPS** (root + www, valid certs, Namecheap→Railway). The landing page is publicly reachable
- ⚪ **Set up `hello@elsewheretel.co` mailbox/routing** (needs doing, not now). The `/legal` page names it as the deletion/contact channel — legally it must actually receive mail before that page counts as a real erasure route. Wire up forwarding (or swap for the address you want) before the `/legal` page is relied on / before the community post
- ⚪ **Signup notification mechanism** (outbound SMS/call = A2P/TCPA) — defer, manual round at friend-scale

## B. Strategy & concept
The vision, positioning, business model, pitch. Mostly docs.

- 🟢 **Full-repo review FILED** (2026-07-24, `reviews/2026-07-24-full-repo-review.md`) — pre-World-0 checkpoint, seven parallel reviewers (code+schema · docs consistency · strategy/economics · world/canon/personas · compliance vs the 8 rules · World 0 readiness · in-world logic). 42 findings in 5 tiers. Headlines: 3 self-declared safety/consent gates already breached on the public numbers (P0); World 0 is specced LLM-free so it cannot move the Stage-0 "are the personas fun" gate (F8); `seed_numbers` has zero code refs so the kill metric is uncomputable (F9); the cannot-lie / stay-in-character / never-claim-sentience trilemma has no answer to "are you an AI?" (F14); the Devil-wall voice (Glenda vs Operator) is the VO critical path (F17). Also: build is clean, link graph is clean, schema/code agree. **TRIAGED 2026-07-24** (4 verifiers; adjudication addendum appended to the review doc). P0 gates ratified accepted-risk for seed; 5 findings overstated; F25/F27 are recorded 2.0a work. New `docs/reviews/` folder seeded
- 🟢 **Strict linting ENFORCED — zero tolerance from day one** (2026-07-24). `eslint.config.mjs` adapted from Borker (minus React/Next): `no-explicit-any` = error (all wire events now typed, no `any` anywhere), strict TS rules, no-non-null-assertion, consistent-type-imports, curly/eqeqeq/prefer-const. Zero violations (Borker waited → ~1500 backlog; we don't inherit it). `no-console` off (server = stdout is observability). `bun run lint` folded into `check` + CI
- 🟢 **session.ts testability refactor + F31** (2026-07-24). Pure relay logic (F29 barge-in state machine + cost estimate) extracted to `relay.ts` and unit-tested against synthetic event sequences — the F29 tail-barge-in case is now a permanent regression guard. F31: the cold opener now waits for the `session.updated` ack instead of firing blind; an error before that ack (a rejected `session.update` = wrong audio format = 5 min of noise) aborts the call loudly instead of silently. All wire events typed (no `any`). Still untested (flagged in strategy doc): the cap/nudge timers + finalize race + F31 ack flow *end-to-end* — need fake timers or an injectable clock
- 🟢 **Test system ESTABLISHED — TDD from now on** (2026-07-24). Modelled on Borker (`social-automation/tests/`): Bun native runner, layered `tests/{unit,integration}/`, `bunfig.toml` preload for deterministic env, strong exact assertions. 52 tests green (45 unit + 7 integration) covering the security-critical logic (F5 signature vs Twilio's published vector + stream token, F33 guards, F7 redaction), the relay barge-in/cost logic (F29), TwiML escaping, persona non-negotiables (openers, canon phrase, the fixed devil loops), and the bridge HTTP contract (403 unsigned / 200 signed, `/legal` serves). `bun run check` = typecheck + lint + suite; CI (`.github/workflows/ci.yml`) gates on it. Strategy + honest gaps: `tests/TESTING_STRATEGY.md`. CLAUDE.md conventions added
- 🟢 **Doc-structure discipline ENFORCED** (2026-07-24) — standing rule: on every new doc, review the docs/ tree + add structure if needed + update all READMEs/refs in the same change set. Three layers: CLAUDE.md rule, memory, and a PostToolUse(Write) hook (`.claude/hooks/new-doc-structure-check.sh`) that reminds on any new `.md` under docs/
- 🟢 **Docs: per-world subfolders** (2026-07-24) — `world/` root = generic cross-world docs (worlds/canon/persona-design ×2); each world gets its own folder (`world/switchboard/` = World Zero, holds switchboard + teaser docs). READMEs per folder; refs updated. Also standardized the connection lead-in to **one ring** everywhere (was 2-3)
- 🟢 **Docs restructured by domain** — `docs/{strategy,engineering,world}/` + README index in each; `docs/README.md` is the front door; `tracker.md` stays at root. All cross-refs (CLAUDE.md, README, code comments) updated
- 🟢 **Business model + economics** captured (`strategy/business-model.md`) — revenue streams, pricing fiction, unit economics; **in-lore native sponsorship** idea recorded (the Devil shills Pepsi, resents it)
- 🟢 **Fundraising** captured (`strategy/fundraising.md`) — media-company thesis, Gimlet-lineage comps + strategic-partner/investor angle, raise-on-retention, timing
- 🟢 **Go-to-market** captured (`strategy/go-to-market.md`) — mystery-as-marketing, the (806) line as the pipe, channels, the 3 pitch decks (public/investor/partner), discipline gate
- 🔴 **Artizen Telegram teaser** — draft ready (`strategy/outreach-log.md`), framed light for the Renee/investor optic. Sven to send, then log `/teaser-stats` + feedback. First non-seed audience test
- 🟢 Outreach log started (`strategy/outreach-log.md`) — track every community push + results
- 🟢 **Vision v2 RATIFIED** (`strategy/vision-world-company.md`) — redlines resolved 2026-07-23. Terminology locked (Elsewhere/Exchange/Management/world/line/**Resident**/subscriber/Ledger/back-channel); Management = authorial mask + diegetic changelog; surveillance/observe-don't-lecture theme; cross-district shared timeline core; staged guardrails (call = unit of delivery not story); reachable vs referenced cast; feeling-first cost spectrum w/ characterized latency; phone-number identity (claim codes retired); knowledge-as-save-state; back-channel (async world→subscriber); two-channel diegetic marketing; hardware phones = dedicated physical object; Phreak district = future candidate
- 🟢 Early tester signal (2026-07-24): "very intriguing" ×multiple; spontaneous **Meow Wolf** association (our comp), one "it was fun, I pressed 1." New comp surfaced: **Ministry of Awe** (Philadelphia, phone-based immersive). This is the "understood there's a world" signal, not novelty
- 🟢 World One locked: the Underworld
- 🟢 Pitch drafted (one-liner / 30s / 90s) — tested informally, "good"
- ⚪ Artizen framing (art-drop launch, funding, AR/VR collaborators) — later

## C. MVP build (World One)
Per `engineering/mvp-2-plan.md`. The actual product.

- 🟢 Runtime persona config (3 levers: model / voice / personality) — DB-driven, no redeploy
- ⚪ **Background-chatter / turn-detection tuning** (filed 2026-07-24, Sven's live observation; related to F29). The live AI reacts to background voices — server_vad treats nearby speech as the caller's turn. And now that F29 makes barge-in fire correctly, a false VAD trigger from background noise will *cut off the persona*, so this matters more, not less. Levers, cheapest first: (a) **VAD threshold** — raise toward 1 to need louder/closer speech (now env-tunable: `VAD_THRESHOLD`/`VAD_SILENCE_MS`/`VAD_PREFIX_MS`, no redeploy needed, tune empirically on real calls); (b) **input noise reduction** (`near_field` for a handset held to the face) — now UNBLOCKED: F31 shipped (2026-07-24), so a wrong/unsupported field no longer fails silently — a rejected `session.update` aborts the call loudly. Safe to try `near_field` now and watch for the abort log; (c) **semantic_vad** turn detection — model-decided turn boundaries, closest to Sven's "is this still relevant / did someone actually address me" ask, but bigger change. Fold the tunable ones into the persona_config lever system so it's per-persona. Not blocking the switchboard
- 🟡 Stage 2.0a — **Devil rebuilt**: canon v2 + Devil bible + mechanics (opener kit, silence timer, day-mood). UNBLOCKED (vision v2 + canon v2 ratified). **Sequenced AFTER the switchboard** (decided 2026-07-24 — see queue item 1): the switchboard's Devil branch is Glenda's receptionist wall, not a live Devil, so the bible + live Devil are a separate later track and do NOT block World Zero
- ⚪ Stage 2.0b — Subscribers (identity = **phone number** + PIN on shared devices; per-district memory. NO claim codes)
- ⚪ Stage 2.0c — First overworld pieces (one IVR, one voicemail box, one secret extension)
- ⚪ Stage 2.0d — Launch surfaces (landing page, Yellow Pages v0)
- ⚪ Back-channel (async world→subscriber: text/voicemail/callback) — consent-gated (A2P/TCPA), lands w/ 2.1

## D. Persona & world writing
The IP: prompts, canon, lore, the transcript flywheel.

- 🟢 **No self-owned crisis guardrails — RATIFIED for seed phase** (2026-07-24, folds into the `types.ts:80-86` note). We removed the crisis/wellbeing language because it bled into the comedy, and we rely on OpenAI's model-level safety for now. **What's accepted:** a caller in real distress gets the persona's bit with no detection, no resource handoff, and no human eyes until Sven's transcript review; the model backstop is tuned to refuse harmful content, not to catch distress delivered to a comedic persona we've told to stay in character. Worst realistic case at seed scale is reputational, not legal (negligence theories need harm + causation, remote at ~20 friends). **Same expiry triggers as the recording ratification** — redraws the deferral's own gate from the already-crossed "informed seed testers" to "before any push," and holds it there. See future-feature item below
- ⚪ **FUTURE FEATURE: self-owned safety layer** (queued 2026-07-24, gated by the expiry triggers above). Reinstate a minimal, scoped crisis-guardrail line (that does NOT bleed into comedy — the reason it was pulled) + a transcript-review distress flag + the kid-in-transcript delete habit. Also unwritten and belongs here: a deadpan, non-denying **"are you an AI?"** answer reconciling cannot-lie + stay-in-character + never-claim-sentience (F14 — bible work; the canon escape hatch already exists, `canon.md:88` "never admitted by the Devil himself"), and distress handling beyond the one sentence in `devil.ts:24`. This is the concept-rule-7 debt; it comes due at the first non-seed caller
- 🟢 Persona format + 4 stubs; design process v1; design notes (calls 1-3) — SUPERSEDED note: the P3 "prompt writing" review findings (F24 diagnosed anti-tics still shipping, F25 opener rotation, F27 canon layer) are **recorded 2.0a work**, not gaps — the opener kit (bridge-rotated) and the shared canon layer are both in `mvp-2-plan.md` Stage 2.0a, and canon v2 only ratified 2026-07-24. Present single-opener / pre-canon-prompt state is the plan. The one live exception was hotfixed 2026-07-24: F24's `devil.ts:17,28` diagnosed loops (socks-list + verbatim notary gag) → rewritten as *described behaviors* ("invented fresh each time, never a repeated list"; "vary how you reach for him"), matching the canon "facts not scripts" rule. Guarded by a regression test (`tests/unit/personas.test.ts`). Full Devil rewrite still deferred to 2.0a
- 🟢 **Devil visual aesthetic LOCKED** (`world/underworld/devil-visual-aesthetic.md`) — the weary amber-lit mid-century clerk with the horned shadow (human, sinister-charming, not a cartoon devil). Derived a style guide + kept the "Call Me Maybe" poster series (v1-v3, v3=hero) and the exact reproduction prompts, all version-controlled. New `world/underworld/` folder seeded (World One)
- 🟢 Devil dark-bait deflection added
- 🟢 **Voice casting DECIDED for the switchboard** (2026-07-24, F17). The existing produced ElevenLabs voice (the teaser's "Elsewhere Operator") becomes **Glenda's** voice for the receptionist wall — its script (the current teaser, "he's off gallivanting…") already fits, needs only a small tweak. The **Operator** (World Zero's host/router) gets a **NEW** Voice-Designed voice — real ElevenLabs→master design work, folded into the switchboard build. NOTE: this re-labels the already-produced asset (below) from "Operator" to "Glenda"; canon `canon.md:115` (Glenda = receptionist wall) is the reference. Switchboard docs still say "Operator" on the wall — Sven's switchboard.md redline resolves that
- 🟢 **Teaser SHIPPED (final production)** — ElevenLabs "Elsewhere Operator" (smoky-2 Voice Design) reading v2 script, over living-switchboard bed (eerie drone + chatter + laugh + weep + dial tone), intro'd by real vintage-phone pickup foley (Sven's recording) + synth ringback. Mixed in Audacity by Sven, telephone-mastered. confirm + goodbye also in operator voice. Live on (806) 666-1212
- 🟢 Teaser script DECIDED v2 — depth pass: elsewhere-as-universe, agency, scale, ambience
- 🟡 Devil bible — UNBLOCKED (vision v2 ratified). Core/registers/mood; resolve reachable-cast roster (secretary? notary?) here; Mrs. Devil stays an undefined slot
- 🟢 **Establishing layer** added to universe architecture (vision §5c/§5-ontology/§8) — pre-produced scene-setting (ambient bed + optional narration) that stages the threshold into a Resident; voice-only's stage directions. Zero live cost, bookends live encounters (short live time → rich scene). Open: per-world narrator vs one signature narrator; Operator does the handoff
- 🟢 Worlds register started (`world/worlds.md`) — all districts (Underworld/Kitchen/2036/divine/Phreak) + NEW seed: **the Philosophical realm** (Sven's — big questions, philosophers as sparring Residents; flagged: modern names Camus/Sartre legally out, flavor via invented Residents). Needs a proper brainstorm later
- 🟢 Canon v2 RATIFIED (`world/canon.md`) — Sven's redline in. THE ONE RULE: canon is facts/stances, never scripts (examples illustrate voice; the thin-Devil ping-pong failure). Locked: subscriber's phone number = their line = effectively their soul; "calls monitored for quality assurance" (propagated to landing+directory surfaces); Devil cannot lie (never self-admitted); prices/souls-on-schedule = generative seasoning not cornerstones; intake = procedure not storyline; Glenda the secretary IN v1 (= the switchboard receptionist wall; may-or-may-not be Mrs. Devil); the Devil has NO name — the world calls him affectionate-demeaning folk diminutives (Serpie/Luzi/Beezy/Old Grump…), terrified he hears (he does). Operator nameless (revisitable); down-trunk kept (leans technical, revisitable). Dead `[[ ]]` → working anchor links. Devil bible unblocked
- ⚪ Weekly transcript-review loop (lore harvest → thin-wall detection → ratify → canon)

---

## Open for Sven (consolidated decision queue)
1. **Full-repo review TRIAGED** (`reviews/2026-07-24-full-repo-review.md` + its 2026-07-24 adjudication addendum) — 42 findings cross-checked by 4 verifiers. Outcomes: P0 recording/crisis gates **ratified accepted-risk for seed** (workstream A/D) with mitigations queued to the community-post trigger; 5 findings overstated (F12/F13/F22/F23/F42); F25/F27 are recorded 2.0a work. **Resolved by Sven 2026-07-24:** sequence = **switchboard first** (F8 — the Devil bible / live Devil is a separate later track, NOT a blocker; door 1 = Glenda's receptionist wall); Devil-wall voice (F17) = **Glenda keeps the existing produced "Elsewhere Operator" voice** (script needs only a small tweak), and the **Operator gets a NEW voice** (needs Voice-Design work, folded into the build); recording disclosure = **in-character in the Operator's opening line** (NOT a generic pre-roll — Sven: stay in fiction always)
2. **Redline `world/switchboard/switchboard.md`** ← Sven's task, the last design pre-work (the A–E brainstorm). Resolve: menu set/order (A — note F23's "at least one door must lead *in*", e.g. promote the D5 wrong-number bit, is input here, not a defect) · Devil-wall copy (B — Glenda, existing voice, tweak the script) · fall-through (C) · easter egg (D) · Operator name (E) · data-model sign-off. Fold in: the Operator's **knowledge boundary** (F16 — one paragraph: does she know where lines lead, does she know callers; "press 1 → Glenda" already means she needn't know the Devil's whereabouts), the **in-character recording disclosure** in her cold-open (Sven's line: "management records all of this — not that anyone expected otherwise… quality assurance… yeah right"), and whether a district is a **phone number or a menu node** (shapes the data model). Then → build V0 switchboard = production Phase 0
3. **Send the Artizen teaser** — approve/tweak the draft (`strategy/outreach-log.md`) and post to the Artizen Telegram; then log results. ⚠️ **HOLD until the community-post mitigation bundle ships** (workstream A) — Artizen is the real event horizon: it's our highest-fit non-seed audience (one first impression), it's the trigger that ends the recording/crisis ratifications, and it breaks the share metric unless a per-campaign attribution field lands first (F10 — a broadcast makes "not in `seed_numbers`" stop meaning "organic share")
4. **Re-ping Erick + the Meow Wolf friend** — their press-1 was lost to the (now-fixed) capture bug; ask them to call once more so it records
5. **Send to more friends** — text (806) 666-1212, watch `/teaser-stats` (now a clean measurement). Also: populate `seed_numbers` (F9) — it's both the kill metric AND the legal tripwire (how "first organic caller" becomes detectable at all)
6. _(decided — phased path adopted)_ **World Graph** Phase 0 folds into the switchboard build (build it schema-first); Phase 1 map (interactive read-only + status overlay) follows. Full pipeline spec'd, deferred. No open decision — sequences behind the switchboard build. See `engineering/production-tools/`

## Cadence
Commit per change set; push in intervals; this tracker updated each working session. Real hygiene (branches/CI/PRs) when we exit prototype phase.
